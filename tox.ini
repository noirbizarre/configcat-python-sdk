[tox]
envlist = py3{7,8,9,10,11}
passenv = LD_PRELOAD
; isolated_build = True
# requires = tox-poetry

[testenv]
deps =
    pytest
    pytest-cov
    mock

commands =
    pytest --cov=configcatclient configcatclienttests
allowlist_externals =
    pytest
